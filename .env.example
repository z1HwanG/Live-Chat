# 复制为 .env 并填写真实值（部署脚本会自动生成）
# 数据库专用账号（由 scripts/init-db.sql 创建，密码要改！）
DB_USER=livechat
DB_PASSWORD=请修改
# 推流密钥（OBS 推流 URL 里的 token）
PUSH_TOKEN=请修改
# 管理员口令
ADMIN_PASSWORD=请修改
# 公网域名（替换为你的域名，用于生成 RTMP 推流地址）
PUBLIC_HOST=live.example.com
# 站点名
SITE_NAME=我的直播
